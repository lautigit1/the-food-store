# Acceptance Criteria: 001-backend-auth-module

- Se puede crear un usuario enviando un POST a `/api/auth/register`.
- La contraseña no se guarda en texto plano en la BD.
- Se puede iniciar sesión enviando credenciales válidas a `/api/auth/login`.
- El endpoint de login devuelve los datos del usuario.
